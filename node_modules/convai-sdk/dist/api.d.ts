import { ChannelUser, MergeUsersRequest, SuperUser, UserQuery, UserQueryReachableResult, UserQueryResult } from "./user-query";
import { BroadcastInput, BroadcastResult } from "./broadcast";
import { UpdateUserDataInput } from "./update-user-data";
import { ExecutionMatcher } from "./execution-query";
import { Execution, ExecutionQueryResult } from "./execution";
import { TriggerRequest } from "./trigger";
import { Session } from "./session";
export declare class ConvaiAPIClient {
    private baseURL;
    private apiKey;
    constructor(apiKey: string);
    private makeRequestWithBody;
    queryExecutions(query: ExecutionMatcher): Promise<ExecutionQueryResult>;
    trigger(req: TriggerRequest): Promise<Execution>;
    broadcast(input: BroadcastInput): Promise<BroadcastResult>;
    queryUsers(query: UserQuery): Promise<UserQueryResult>;
    queryUsersReachable(query: UserQuery): Promise<UserQueryReachableResult>;
    mergeUsers(req: MergeUsersRequest): Promise<SuperUser>;
    deleteSuperUser(id: string): Promise<SuperUser>;
    updateUserData(superUserId: string, input: UpdateUserDataInput): Promise<SuperUser>;
    deleteChannelUser(userId: string): Promise<ChannelUser>;
    updateSession(userId: string, input: UpdateUserDataInput): Promise<Session>;
    deleteSession(userId: string): Promise<Session>;
    setBaseURL(baseURL: string): void;
}
