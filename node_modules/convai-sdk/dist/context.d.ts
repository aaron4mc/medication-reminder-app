import { Flaggable } from "./flaggable";
import { Session } from "./session";
import { Error } from "./context-modifier";
import { Response } from "./response";
export declare class Context extends Flaggable {
    id: string;
    user: RequestUser;
    session: Session;
    text: string;
    channel: string;
    source: any;
    isStart: boolean;
    isTrigger: boolean;
    errors?: Error[];
    response?: Response;
    constructor();
    /**
     * Converts from a plain js object into a Context class
     * @param obj an object with the same fields as a context
     */
    static fromObject(obj: any): Context;
}
export declare class RequestUser extends Flaggable {
    id: string;
    channelId: string;
    constructor();
}
